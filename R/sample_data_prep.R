
library(sf)
#elk <- st_read(dsn='data', layer='points')

library(dplyr)
#elk <- filter(elk, fix_type_r %in% c('3D Fix','3D')) %>% select(c('long','lat','ind_ident','study_ts'))

#st_transform(elk, crs=sp::CRS('3400'))
